let array = []
let contador = 0
let confirmar = 0

while (confirmar <= 10) {
    let numero = parseInt(prompt("Insira um número inteiro e positivo"))
    array [contador] = numero

    contador++
}

for (let i = array [0]; i >= array [0]; i++) {
    for (let g = array; g <= array; g+= array) {
        console.log (array[i])
        console.log (array[g])  
    }
}
